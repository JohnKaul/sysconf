# test header.
/**
 * My configuration test file.
 *
 * This is a test header
 * with multiple lines written
 * in a c-style comment.
 */

[config]
  example = examplevalue; # test comment.

[settings]
 value1=test2;                  # test comment.
     value2 = test2;

/**
 * test comment
 */
config {
       value3 = test3;
       value3=test4;            # no space between equal.
   exec.start = /bin/sh /etc/rc;
    exec.stop = /bin/sh /etc/rc.shutdown    # no terminating sym.
     exec.consolelog = /var/log/jail_console_${name}.log;
}
 config2 {
         value4=test3;           # comment
         value5=test4;
         exec.consolelog = /var/log/jail_console_${name}.log;
 }

key6 = value6;
        key7 = value7;
                key8 = value8;
key9 = value9;

jackson         = (plus-equal) cool kind sweet generious;            # a comment
jace            = (equal) cool sweet kind caring generious;     # a comment

# jackson         : (colon) cool kind sweet generious;
# jace            : (colon) cool sweet kind caring generious;

jenny   wife                                                    # a comment
